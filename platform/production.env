# Non-secret production environment values for home-portal.
# Secrets NEVER live here — they are delivered by the platform secret store.
# Rendered into the workload fragment by deploy-config-schema.
